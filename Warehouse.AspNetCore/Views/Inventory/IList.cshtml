@model IEnumerable<Warehouse.AspNetCore.Models.Inventory>


@{
    ViewData["Title"] = "Item List";
}

<p>
    <a asp-action="InCreate">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ItemId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShortDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LongDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InStock)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>

@foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShortDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LongDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InStock)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category)
            </td>
            <td>
                <a asp-action="IEdit" asp-route-id="@item.ItemId">Edit</a> |
                <a asp-action="IDetails" asp-route-id="@item.ItemId">Details</a> |
                <a asp-action="IDelete" asp-route-id="@item.ItemId">Delete</a>
            </td>
        </tr>
        }
    </tbody>
</table>





@*@foreach (var inventory in Model)
    {
        <div class="col-sm-4 col-lg-4 col-md-4">
            <div class="img-thumbnail">
                <img src="@inventory.ImageThumbnailUrl" alt="Missing" />
                <div class="caption">
                    <h3>
                        <a asp-controller="Inventory" asp-action="IDetail" asp-route-id="inventory.ItemId">@inventory.Name</a>
                    </h3>
                    <h3>@inventory.Price.ToString("c")</h3>
                    <h3>@inventory.Category.CategoryName</h3>
                    <p>inventory.ShortDescription</p>
                </div>
            </div>
        </div>
    }*@


